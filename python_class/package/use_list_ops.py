from package.list_ops import add, subtract, multiply, divide

foo = [1, 2, 3, 4, 5]
bar = [24, 52, 13, 27]
result = add(foo, bar)
print(f"{foo} + {bar} = {result}")
result = multiply(foo, bar)
print(f"{foo} + {bar} = {result}")

